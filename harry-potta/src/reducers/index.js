
const initialState = {
    harrypotterhouses: [],
    isLoading: true, 
};

export const harryPotterReducer = (state = initialState, action) => {
    switch (action.type) {

        default: 
        return state;
    }
}