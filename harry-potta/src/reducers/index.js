
const initialState = {
    harrypotterhouses: [],
    test: "testing - it's working"
};

export const harryPotterReducer = (state = initialState, action) => {
    switch (action.type) {

        default: 
        return state;
    }
}